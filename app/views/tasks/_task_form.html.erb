<% form_for [category,task], :html => {:id => id, :class => "do"} do |f| -%>
  <%= f.label :name, "Task Title:" %>
  <%= f.text_field :name %>
  <%= f.submit "Add Task" %>
  <%= hidden_field_tag :category, @category.id %>
<% end -%>